/* Commands to connecto the local host via the CLI and create the required columns
connect localhost/9160

/* Create a new keyspace */
create keyspace Lucandra with replication_factor=1 and placement_strategy=org.apache.cassandra.locator.SimpleStrategy

/* Switch to the new keyspace */
use Lucandra

/* Create new column families */
create column family Documents with column_type='Standard' and comparator='BytesType'

create column family TermInfo with column_type='Super' and comparator='BytesType' and subcomparator='BytesType'
